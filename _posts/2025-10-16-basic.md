---
layout: post
title: "Basic"
date: 2023-01-16
categories: [general]
tags: [React]
---


```javascript
function App() {
  const name = 'react';
  return (
    <>
    {name === 'react' && <h1>리액트입니다</h1>}
    </>
  );
}
//리액트입니다

export default App;
```


🌀 name 이 react 면 && 뒤의 값 출력, 아니라면 null 출력됨 / react 에서는 false 를 렌더링 할 때 아무것도 나타나지 않기 때문에


```javascript
function App() {
  const name = undefined;
  return name || '값이 undefined 입니다'
}
//값이 undefined 입니다

export default App;
```


```javascript
function App() {
  const name = undefined;
  return <div>{name || '리액트'}</div>
}
//리액트

export default App;
```


🌀 undefined 만 반환하여 렌더링 하면 오류 발생 / || 연산자로 해당 값이 undefined 일 때 사용할 값 지정 가능


```javascript
function App(){
  const name = '리액트';
  const style = {
    backgroundColor : 'black',
    color : 'aqua',
    fontSize : '48px',
    fontWeight : 'bold',
    padding : 16
  }
  return <div style={style}>{name}</div>
}

export default App;
```


🌀 리액트에서 스타일 속성 주기


```javascript
import './App.css'

function App(){
  const name = '리액트';
  return <div className='react'>{name}</div>
}

export default App;
```


🌀 class 대신 className 사용


```javascript
import './App.css'

function App(){
  const name = '리액트';
  return (
    <>
      {/* 주석은 이렇게 작성합니다 */}
      <div
      className='react' //시작 태그를 여러 줄로 작성하게 된다면 여기에 주석을 작성 할 수 있습니다
      >
        {name}
      </div>
    //하지만 이런 주석이나
    /*이런 주석은 페이지에 그대로 나타나게 됩니다*/
    <input></input>
    </>
  )
}

export default App;
```


🌀 주석 다는 방법

