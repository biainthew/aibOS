---
layout: post
title: "자바의 자료형과 연산자"
date: 2024-09-29
categories: [general]
tags: [Java]
excerpt_separator: ""
---



### 이스케이프표


```javascript
String str1 = "문자열에 "큰따옴표가" 안 들어가요";
        String str2 = "엔터도 안 들어가요. 못 믿겠으면 넣어봐.";
```


| **이스케이프 표현** | **대체**  |
| ------------ | ------- |
| `\"`         | 큰따옴표    |
| `\’`         | 작은따옴표   |
| `\n`         | 줄바꿈     |
| `\t`         | 탭       |
| `\\`         | 백슬래시 하나 |


```java
//  실행하여 확인
        String str = "문자열에 \"큰따옴표\"도,\n엔터도 넣을 수 있다고?";
        System.out.println(str);
```


```java
String table = "언어\t\t종류\t\t\t\t자료형\nJava\tcompiled\t\t정적\nPython\tinterpreted\t\t동적";
        System.out.println(table);
```


```java
char singleQuote = '\'';
        System.out.println(singleQuote);

        String path = "C:\\Document\\MyCodings";
        System.out.println(path);
```


## 문자열의 메소드들


**문자열은 불변 immutable**

- 문자열 자체를 변경하는 메소드는 없음
    - 새 문자열 또는 다른 값을 반환
    - 문자열 변수에 다른 값을 넣을 수 없다는 뜻은 아님

### .length


문자열 길이 반환


### .isEmpty / .isBlank


isEmpty : 문자열의 길이가 0인지 여부


isBlank : 공백을 제외한 문자열의 길이가 0인지 여부


### .trim


앞 뒤의 공백 제거


> 💡 **변수 자체의 값을 바꾸려면?**  
> `str3 = str3.trim()`


### .charAt - char


~번째 문자 반환


```java
//마지막 문자 얻기

char ch3 = str1.charAt(str1.length()-1);
```


### .indexOf - int


일치하는 첫번째/마지막 문자열의 위치


```java
String str2 = "얄리 얄리 얄라셩 얄라리 얄라";

        //  💡 indexOf/lastIndexOf : 일치하는 첫/마지막 문자열의 위치
				//  앞에서부터 카운트해서 int로 반환

        //  두 번째 인자 : ~번째 이후/이전부터 셈
        int int1 = str2.indexOf('얄');
        int int2 = str2.indexOf('얄', 4);

        int  int3 = str2.indexOf("얄라");
        int  int4 = str2.lastIndexOf("얄라");
        int  int5 = str2.lastIndexOf("얄라", 12);

				//  💡 포함되지 않은 문자는 -1 반환
        int int6 = str2.indexOf('욜');
```


### .equals / .equalsIgnoreCase - boolean


값 동일 여부 / 대소문자 구분 없이


### .contain / .startsWith / .endsWith - boolean


포함 여부 / 시작 여부 / 끝남 여부


### .matches - boolean


정규식 일치 여부


### .compareTo / .compareToIgnoreCase - int


같은 문자열이면 0


시작하는 부분이 같을 때는 글자 길이의 차이


시작하는 부분이 다를 때는 첫 글자의 정수값 차이


### .toUpperCase / .toLowerCase


### .concat - String


문자열 연결


string을 반환하는 것들은 메서드를 연결해서 계속 쓸 수 있음


```java
⭐️ 메서드 체이닝
  String str_a6 = str_a1
          .concat(str_a2)
          .concat(str_a3)
          .concat(str_a4)
          .concat(str_a5);
```


**+ 연산자와의 차이**

1. concat에는 문자열만 이어붙일 수 있음
2. concat은 필요시에만 새 인스턴스 생성
3. + 연산자는 null과 이어붙이기 가능, concat은 NullPointerException 발생
4. 성능은 +가 더 좋음

```java
//  4. ⭐️ 다중 연산시 생성되는 문자열 인스턴스의 수가 다름

    String str_d1 = "a" + "b" + "c" + "d";

    // + 연산은 내부적으로 아래와 같이 최적화됨 (이후 배움)
    String str_d2 = new StringBuilder("a")
            .append("b")
            .append("c")
            .append("d")
            .toString(); // "abcd"가 생성됨
    // "a", "b", "c", "d", "abcd"가 생성되어 메모리 차지

    //  concat은 매 번 문자열을 반환하므로
    String str_d3 = "a"
            .concat("b") // "ab"가 생성됨
            .concat("c") // "abc"가 생성됨
            .concat("d"); // "abcd"가 생성됨
    // "a", "b", "c", "d", "ab", "abc", "abcd"가 생성되어 메모리 차지
```


### .repeat


```java
String str_a1 = "덜컹";

    // 💡 repeat : 문자열을 주어진 정수만큼 반복
    String str_a2 = str_a1.repeat(2);
    String str_a3 = str_a1
            .concat(" ")
            .repeat(3)
            .trim();
```


### .substring


잘라오기


```java
String str_b1 = "대한민국 다 job 구하라 그래";

    //  💡 substring : ~번째 문자부터 (~번째 문자까지) 잘라서 반환
    String str_b2 = str_b1.substring(7);
    String str_b3 = str_b1.substring(7, 10);
    String str_b4 = str_b1.substring(11, 14);

    String piece1 = "다 ";
    String piece2 = "구하라";
    String str_b5 = str_b1.substring(
            str_b1.indexOf(piece1),
            str_b1.indexOf(piece2) + piece2.length()
    );
```


### .replace


치환


```java
//  💡 replace : 주어진 앞의 문자(열)을 뒤의 문자(열)로 치환
    String str_c1 = "점심에 보쌈집에 가서 수육을 먹었다.";
    String str_c2 = str_c1.replace("보쌈", "중국");

    //  여럿 포함시 모두 치환
    String str_c3 = "밥 좀 먹자, 응? 야, 밥 좀 먹자고 밥 밥 밥";
    String str_c4 = str_c3.replace('밥', '빵');

    // ⭐️ 원본은 바뀌지 않는 점 확인
```


```java
//  메서드 체이닝
    String str_d1 = "하여튼 호의가 반복되면 권리인 줄 알아";
    String str_d2 = str_d1
            .replace("하여튼", "아무튼")
            .replace("호의", "호이".repeat(2))
            .replace("권리", "아기공룡 둘리");
```


```java
String str_e1 = "02=123.4567_8900";

    //  💡 replaceAll / replaceFirst : ⭐️ 정규표현식 사용 가능
    //  전부 치환 / 첫 번째 일치부분만 치환
    String str_e2 = str_e1
            .replaceAll("[=._]", "-")
            .replaceFirst("[-@#]", ")");
```


### .toCharArray / .split


문자열을 분할하여 배열로 반환

