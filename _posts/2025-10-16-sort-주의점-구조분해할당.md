---
layout: post
title: "sort( ) 주의점 / 구조분해할당"
date: 2023-05-09
categories: [general]
tags: [Javascript/Jquery]
excerpt_separator: ""
---



### Sort 함수는 원본 배열을 바꾼다


오름차순, 내림차순 정렬 등에 많이 사용하는 `sort` 함수는 원본 배열을 바꾼다 전혀 몰랐음


```javascript
let newArr = arr.sort((a,b)=>a-b)

//이렇게 해두고 arr 과 newArr을 비교해보면 같음
```


### 해결법 - 구조분해 할당


원본 배열을 유지하고 새로운 배열을 만들어서 `sort` 를 적용시키면 된다


```javascript
let newArr = [...arr].sort((a,b)=>a-b)
```


> 💡 **구조분해할당 ?  
> -** 배열이나 객체의 속성을 해체하여 그 값을 변수에 담을 수 있게 하는 표현식


### 구조분해할당 예시


```javascript
let a, b, rest;
[a, b] = [10, 20];
console.log(a); // 10
console.log(b); // 20

[a, b, ...rest] = [10, 20, 30, 40, 50];
console.log(a); // 10
console.log(b); // 20
console.log(rest); // [30, 40, 50]

({ a, b } = { a: 10, b: 20 });
console.log(a); // 10
console.log(b); // 20

// Stage 4(finished) proposal
({a, b, ...rest} = {a: 10, b: 20, c: 30, d: 40});
console.log(a); // 10
console.log(b); // 20
console.log(rest); // {c: 30, d: 40}
```


```javascript
var a, b;

[a=5, b=7] = [1];
console.log(a); // 1
console.log(b); // 7
//대입한 배열에 요소가 하나밖에 없어서 b 는 undefined 니까 원래값을 반환함
```


[참고](https://developer.mozilla.org/ko/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment)


## 질문


다 해결함

