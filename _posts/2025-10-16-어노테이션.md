---
layout: post
title: "어노테이션"
date: 2025-01-02
categories: [general]
tags: [Java]
---


### NoArgsConstructor


: **매개변수가 없는** 기본 생성자를 자동으로 생성한다


-> JPA 엔티티, 직렬화/역직렬화를 위해 기본 생성자가 필요할 때 사용


> 💡 **JPA 엔티티 / 직렬화 / 역직렬화**  
> **JPA 엔티티** : 데이터 베이스 테이블에 1대1로 매핑되는 자바 객체  
>   
> **직렬화** : 자바 객체를 바이트 스트림으로 변환하여 파일 저장이나 네트워크 전송에 사용  
>   
> **역직렬화** : 바이트 스트림을 다시 자바 객체로 변환


```java
import lombok.NoArgsConstructor;

@NoArgsConstructor
public class MyClass {
    private String name;
    private int age;
}
```


```java
public MyClass() {}
```


### RequiredArgsConstructor


: `final` 필드와 `@NonNull` 이 지정된 필드만을 포함하는 생성자를 생성한다


-> 필수적인 `final` 필드만 초기화가 필요할 때, 불변 객체를 생성하거나 주입받는 의존성(`@Autowired`)을 설정할 때 유용


```java
import lombok.RequiredArgsConstructor;

@RequiredArgsConstructor
public class MyClass {
    private final String name;
    private int age; // 포함되지 않음
}
```


```java
public MyClass(String name) {
    this.name = name;
}
```


```java
MyClass obj = new MyClass("Alice"); // 'name'만 초기화
```


### AllArgsConstructor


: 클래스의 **모든 필드**를 파라미터로 받는 생성자를 생성한다


-> DTO, VO 같은 클래스에 유용


```java
import lombok.AllArgsConstructor;

@AllArgsConstructor
public class MyClass {
    private String name;
    private int age;
}
```


```java
public MyClass(String name, int age) {
    this.name = name;
    this.age = age;
}
```


```java
MyClass obj = new MyClass("Alice", 30); // 모든 필드를 초기화
```

