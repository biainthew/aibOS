---
layout: post
title: "Node.js script basic"
date: 2023-02-06
categories: [general]
tags: [Javascript/Jquery]
excerpt_separator: ""
---



[: 참고 문서](https://araikuma.tistory.com/453)


# ✔️ `Node.js` 스크립트 기본


# _1) require_


: 필요한 라이브러리 로드


### 🌈 예시


> 💡 `const http = require(’http’);`


👉🏻 HTTP 의 각종 기능을 요약한 http 개체를 로드 / 여기에서 서버 개체를 만든다


# _2) createServer_


: Node.js 의 서버 가 되는 http.Server 개체를 만들어줌 / 나중에 필요한 처리를 작성한다


### 🌈 예시


> 💡 `const server = http.createServer();`


👉🏻 만든 이 개체를 준비하고 필요한 설정을 하고 서버로 실행


# _3) on_


: 이벤트 처리 통합 / 첫번째 인수에 이벤트 이름, 두번째 인수에 통합처리(함수) 를 지정


### 🌈 예시


> 💡 `server.on('request', doRequest);`


👉🏻 request 는 http.Server 객체가 클라이언트의 요청을 받았을 때 발생하는 이벤트로 여기에 브라우저에서 서버에 엑세스 할 때 서버 응답 처리 를 포함


# _4) listen_


: 실행하면 서버는 대기상태가 되고 클라이언트에서 요청이 있으면 그것을 받아 처리할 수 있음


### 🌈 예시


> 💡 `server.listen(1234);`


👉🏻 첫번째 인수 : 포트 번호 / 두번째 인수 : 호스트 이름 / 세번째 인수 : 백로그 / 네번째 인수 : 콜백함수


# _5) console_


### 🌈 예시


> 💡 `console.log('Server running!');`


👉🏻 listen 에서 대기를 시작했던 곳으로 메세지를 콘솔에 출력


# ✔️ 요청 처리를 위한 함수


{% raw %}
```javascript
function 함수명 (req, res) {
    ...... 필요한 처리 ......
}
```
{% endraw %}

- `request` : 클라이언트의 요청에 대한 기능 정리
- `response` : 클라이언트로 리턴되는 응답에 대한 기능 정리

{% raw %}
```javascript
res.writeHead(200, {'Content-Type': 'text/plain'});
```
{% endraw %}

- `writeHead` : response 객체의 메소드에서 정보를 응답에 작성해서 내보냄 / 첫번째 인자는 상태 코드를 지정하고 두번째 인자에 정보를 연관 배열로 정리
- `{'Content-Type': 'text/plain'}` : 이 컨텐츠는 표준 텍스트이다 라는 뜻

{% raw %}
```javascript
res.write('Hello World\n');
```
{% endraw %}

- `write` : 내용 내보내는 것 / 인수에 지정한 값이 컨텐츠로 작성됨 / 여러번 호출 가능

{% raw %}
```javascript
res
.end();
```
{% endraw %}

- `end` : 컨텐츠 출력 완료 / 인수로 내보낼 내용의 값을 지정할 수 있음

### 👇🏻 결과


{% raw %}
```javascript
const http = require('http');

const server = http.createServer();
server.on('request', doRequest);
server.listen(process.env.PORT, process.env.IP);
console.log('Server running!');

// 요청 처리
functiondoRequest(req, res) {
    res.writeHead(200, {'Content-Type': 'text/plain'});
    res.write('Hello World\n');
    res.end();
}
```
{% endraw %}

