---
layout: post
title: "Tab menu"
date: 2022-12-05
categories: [general]
tags: [Javascript/Jquery]
excerpt_separator: ""
---


- 클릭하면 나타나는 탭메뉴
- Css
    - 클릭할 탭 ul>li*n>a class active
        - Ul : display flex
        - Li : width
        - A : display block href=밑에 div id
        - A active : 스타일 속성 다르게
    - 나타나는 탭 div>div*n
        - 밑에 div : display none
        - 밑에 div first child : display block
- Js
    - For 문으로 a의 개수만큼 클릭이벤트
    - A 의 href 속성값에서 # 을 공백으로 replace
    - For 문으로 a의 개수만큼
    - a의 클래스 active 다 지우기
    - E.target에만 클래스 active 추가
    - For 문으로 div개수만큼
    - Div 전체 다 display none
    - A의 href 속성값 지운거 + id 에 display block

    {% raw %}
```javascript
    const targetLink=document.querySelectorAll('.tab-menu a');
    const tabContent=document.querySelectorAll('#tabContent div');
    
    for(i=0; i<targetLink.length; i++){
        targetLink[i].addEventListener('click', (e) => {
            // let orgTarget = e.target.getAttribute('href')
            // let tabTarget = orgTarget.replace('#','')
            let tabTarget = e.target.getAttribute('href').replace('#','')
            // console.log(e.target.getAttribute('href').replace('#',''))
            for(j=0; j<targetLink.length; j++){
                targetLink[j].classList.remove('active');
                e.target.classList.add('active');
            }
            for(k=0; k<tabContent.length; k++){
                tabContent[k].style.display='none'
            }
            document.getElementById(tabTarget).style.display='block'
        })
    }
    ```
{% endraw %}

