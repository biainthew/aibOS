---
layout: post
title: "map"
date: 2023-01-23
categories: [general]
tags: [React]
excerpt_separator: ""
---



# _map( )_


: 파라미터로 전달된 함수를 사용해서 배열 내 각 요소를 원하는 규칙에 따라 변환한 후 그 결과로 새로운 배열 생성


### ✔️ 문법


> 💡 `arr.map(callback, [thisArg])`


👉🏻 callback : 새로운 배열의 요소를 생성하는 함수


👉🏻 callback의 parameter : currentValue(요소), index, array(원본배열)


👉🏻 thisArg : (선택항목) callback 내부에서 사용할 this 레퍼런스


### 🌈 예시


> 💡 `let numbers = [1,2,3,4,5];  
> let processed = numbers.map(function(num){  
>     return num * num  
> });`


👉🏻 기존 배열로 새로운 배열을 만드는 역할


### ✨ 예제


```javascript
const IterationSample = () => {
    const names = ['눈사람', '얼음', '눈', '바람'];
    const name = names.map((name)=>{
        return(
            <li>{name}</li>
        )
    })
    return(
        <ul>{name}</ul>
    )
};
export default IterationSample;
```

